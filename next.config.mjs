/** @type {import('next').NextConfig} */
const nextConfig = {};
// next.config.mjs
export default {
  images: {
    domains: ['firebasestorage.googleapis.com'],
  },
};

// export default nextConfig;
